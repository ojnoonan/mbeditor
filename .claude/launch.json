{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "Dummy App (Rails)",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "export RBENV_ROOT=$HOME/.rbenv && export PATH=$HOME/.rbenv/shims:$HOME/.rbenv/bin:$PATH && cd test/dummy && bundle exec rails server -p 3000"],
      "port": 3000
    }
  ]
}
